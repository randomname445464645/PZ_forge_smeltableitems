diff --git a/conf/conf.yaml b/conf/conf.yaml
index 0538e5c..0e1754a 100644
--- a/conf/conf.yaml
+++ b/conf/conf.yaml
@@ -1,17 +1,17 @@
 pz_root: |- # project zomboid game folder
-    D:/SteamLibrary/steamapps/common/ProjectZomboid
+    /home/kiri/GOG Games/Project Zomboid/game/projectzomboid
 
 output_root: |- # render output folder
-    E:/pzmap
+    /home/kiri/Documents/labo/pzmap/out
 
 mod_root: |- # steam workshop folder for project zomboid
-    D:/SteamLibrary/steamapps/workshop/content/108600
+    /home/kiri/.local/share/Steam/steamapps/workshop/content/108600
 
 custom_root: |- # custom file folder
     .
 
 save_game_root: |- # save game folder
-    %UserProfile%/zomboid/Saves
+    /home/kiri/Zomboid/Saves
 
 # for multi map set
 # output_entry: map set name
@@ -44,6 +44,14 @@ base_map: default
 # Additional overlay maps
 # See mod/ folder for map names.
 mod_maps:
+    - Trelai_B42
+    - Greenport_B42
+    - Maplewood_B42
+    - RavenCreek_B42
+    - Constown_B42
+    - NewHartburg_B42
+    - Chestown_B42
+    - LQZ_B42
 #    - Ashenwood
 #    - BedfordFalls
 #    - Blueberry
@@ -65,8 +73,8 @@ mod_maps:
 #
 # or use 'all' to render all save games with same version of the base map
 # e.g.
-# save_games: all
-save_games: all
+# save_games: []
+save_games: []
 
 render_conf:
     # you can customize each setting under render_conf for each job or mod map by using the following syntax:
@@ -113,7 +121,8 @@ render_conf:
     #     layer_range: [-32, 32]
     # render all possible layers
     #     layer_range: all
-    layer_range: all
+    # sous-sols -2..-1 + rez + 8 etages : couvre tout le loot sauf les tours de Louisville
+    layer_range: [-2, 9]
 
     # hash function for detecting source file changes
     # if omitted or set to null, last modified time will be used for change detection, which is faster but less accurate
@@ -136,7 +145,12 @@ render_conf:
     #         - [0, 18, 45, 45]
     #         - [45, 3, 13, 60]
     #         - [58, 0, 20, 63]
-    dzi_cell_range[default]: all_mod_maps
+    # emprise vanilla figee : les cartes moddees ont leur propre pyramide,
+    # inutile d'agrandir le canevas de base et de tout re-rendre
+    dzi_cell_range[default]:
+        - [0, 18, 45, 45]
+        - [45, 3, 13, 60]
+        - [58, 0, 20, 63]
     dzi_cell_range: auto
 
     # A list of cell ranges to render
diff --git a/pzmap2dzi/cell.py b/pzmap2dzi/cell.py
index 59ad4f4..0cb032e 100644
--- a/pzmap2dzi/cell.py
+++ b/pzmap2dzi/cell.py
@@ -16,6 +16,15 @@ class Cell(object):
         block_num, pos = util.read_uint32(data, pos)
         self.blocks = []
         block_table = pos
+        # Certaines cartes moddees (ex: Trelai_B42) declarent un block_num faux.
+        # On le recalcule depuis la table d'offsets : le premier offset marque
+        # la fin de la table, donc (offset0 - block_table) / 8 entrees.
+        expected = self.block_per_cell * self.block_per_cell
+        if block_num != expected:
+            first_offset, _ = util.read_uint32(data, block_table)
+            derived = (first_offset - block_table) // 8
+            if derived == expected:
+                block_num = derived
         for i in range(block_num):
             pos, _ = util.read_uint32(data, block_table + i * 8)
             block, pos = binfile.read_block(
diff --git a/pzmap2dzi/pzdzi.py b/pzmap2dzi/pzdzi.py
index d2e756d..152897d 100644
--- a/pzmap2dzi/pzdzi.py
+++ b/pzmap2dzi/pzdzi.py
@@ -187,7 +187,10 @@ class DZI(object):
 
     def clear_wip(self, level, x, y):
         ext, path = self.tile_path(level, x, y, 0, 'pending')
-        os.remove(path)
+        try:
+            os.remove(path)
+        except FileNotFoundError:
+            pass
 
     def create_empty_output(self):
         for layer in range(self.render_minlayer, self.render_maxlayer):
